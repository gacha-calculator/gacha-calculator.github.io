
{
  "name": "gacha-calculator",
  "version": "0.1.0",
  "homepage": "https://gacha-calculator.github.io",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "gzip-sitemap": "node scripts/gzip-sitemap.mjs",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist && npm run gzip-sitemap"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
